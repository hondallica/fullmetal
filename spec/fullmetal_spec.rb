require 'spec_helper'

describe Fullmetal do
  it 'has a version number' do
    expect(Fullmetal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
